package com.turbolent.wikidata.filter;

import org.semanticweb.yars.nx.Resource;

final class Schema {
    static final String URI = "http://schema.org/";
    static final Resource NAME = new Resource(URI + "name");
}
